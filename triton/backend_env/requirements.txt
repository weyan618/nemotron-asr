# Runtime dependencies for the Triton Python backend (SERVING only).
# The pipeline (asr_pipeline.py) is NeMo-free and imports just these three.
#
# Python MUST be 3.12 (match the Triton container's python backend stub, e.g.
# nvcr.io/nvidia/tritonserver:25.x-py3). Versions are pinned to match the TRT
# engines (built with TensorRT 10.16.1.11). See ENVIRONMENT.md for full steps.
#
# torch is the CUDA 13.0 build and is NOT on default PyPI — install it from the
# PyTorch cu130 index first, then the rest:
#
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cu130
#   pip install -r requirements.txt --extra-index-url https://pypi.nvidia.com
#
# torch automatically pulls the matching nvidia-* CUDA libs (cudnn/cublas/nccl/…).

torch==2.12.0
tensorrt-cu13==10.16.1.11
numpy==2.4.6
